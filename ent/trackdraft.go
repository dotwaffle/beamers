// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/dotwaffle/beamers/ent/track"
	"github.com/dotwaffle/beamers/ent/trackdraft"
)

// TrackDraft is the model entity for the TrackDraft schema.
type TrackDraft struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// TrackID holds the value of the "track_id" field.
	TrackID int `json:"track_id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Retired holds the value of the "retired" field.
	Retired bool `json:"retired,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TrackDraftQuery when eager-loading is set.
	Edges        TrackDraftEdges `json:"edges"`
	selectValues sql.SelectValues
}

// TrackDraftEdges holds the relations/edges for other nodes in the graph.
type TrackDraftEdges struct {
	// Track holds the value of the track edge.
	Track *Track `json:"track,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// TrackOrErr returns the Track value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TrackDraftEdges) TrackOrErr() (*Track, error) {
	if e.Track != nil {
		return e.Track, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: track.Label}
	}
	return nil, &NotLoadedError{edge: "track"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TrackDraft) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case trackdraft.FieldRetired:
			values[i] = new(sql.NullBool)
		case trackdraft.FieldID, trackdraft.FieldTrackID:
			values[i] = new(sql.NullInt64)
		case trackdraft.FieldName:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TrackDraft fields.
func (_m *TrackDraft) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case trackdraft.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case trackdraft.FieldTrackID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field track_id", values[i])
			} else if value.Valid {
				_m.TrackID = int(value.Int64)
			}
		case trackdraft.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case trackdraft.FieldRetired:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field retired", values[i])
			} else if value.Valid {
				_m.Retired = value.Bool
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TrackDraft.
// This includes values selected through modifiers, order, etc.
func (_m *TrackDraft) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryTrack queries the "track" edge of the TrackDraft entity.
func (_m *TrackDraft) QueryTrack() *TrackQuery {
	return NewTrackDraftClient(_m.config).QueryTrack(_m)
}

// Update returns a builder for updating this TrackDraft.
// Note that you need to call TrackDraft.Unwrap() before calling this method if this TrackDraft
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *TrackDraft) Update() *TrackDraftUpdateOne {
	return NewTrackDraftClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the TrackDraft entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *TrackDraft) Unwrap() *TrackDraft {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: TrackDraft is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *TrackDraft) String() string {
	var builder strings.Builder
	builder.WriteString("TrackDraft(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("track_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.TrackID))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("retired=")
	builder.WriteString(fmt.Sprintf("%v", _m.Retired))
	builder.WriteByte(')')
	return builder.String()
}

// TrackDrafts is a parsable slice of TrackDraft.
type TrackDrafts []*TrackDraft
