// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/dotwaffle/beamers/ent/account"
	"github.com/dotwaffle/beamers/ent/accountprofile"
	"github.com/dotwaffle/beamers/internal/profilevalue"
)

// AccountProfile is the model entity for the AccountProfile schema.
type AccountProfile struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// AccountID holds the value of the "account_id" field.
	AccountID int `json:"account_id,omitempty"`
	// NormalizedHandle holds the value of the "normalized_handle" field.
	NormalizedHandle string `json:"normalized_handle,omitempty"`
	// DisplayName holds the value of the "display_name" field.
	DisplayName string `json:"display_name,omitempty"`
	// Published holds the value of the "published" field.
	Published bool `json:"published,omitempty"`
	// SelectedEntries holds the value of the "selected_entries" field.
	SelectedEntries []profilevalue.Entry `json:"selected_entries,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the AccountProfileQuery when eager-loading is set.
	Edges        AccountProfileEdges `json:"edges"`
	selectValues sql.SelectValues
}

// AccountProfileEdges holds the relations/edges for other nodes in the graph.
type AccountProfileEdges struct {
	// Account holds the value of the account edge.
	Account *Account `json:"account,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// AccountOrErr returns the Account value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e AccountProfileEdges) AccountOrErr() (*Account, error) {
	if e.Account != nil {
		return e.Account, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: account.Label}
	}
	return nil, &NotLoadedError{edge: "account"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AccountProfile) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case accountprofile.FieldSelectedEntries:
			values[i] = new([]byte)
		case accountprofile.FieldPublished:
			values[i] = new(sql.NullBool)
		case accountprofile.FieldID, accountprofile.FieldAccountID:
			values[i] = new(sql.NullInt64)
		case accountprofile.FieldNormalizedHandle, accountprofile.FieldDisplayName:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AccountProfile fields.
func (_m *AccountProfile) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case accountprofile.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case accountprofile.FieldAccountID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field account_id", values[i])
			} else if value.Valid {
				_m.AccountID = int(value.Int64)
			}
		case accountprofile.FieldNormalizedHandle:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field normalized_handle", values[i])
			} else if value.Valid {
				_m.NormalizedHandle = value.String
			}
		case accountprofile.FieldDisplayName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field display_name", values[i])
			} else if value.Valid {
				_m.DisplayName = value.String
			}
		case accountprofile.FieldPublished:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field published", values[i])
			} else if value.Valid {
				_m.Published = value.Bool
			}
		case accountprofile.FieldSelectedEntries:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field selected_entries", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.SelectedEntries); err != nil {
					return fmt.Errorf("unmarshal field selected_entries: %w", err)
				}
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the AccountProfile.
// This includes values selected through modifiers, order, etc.
func (_m *AccountProfile) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryAccount queries the "account" edge of the AccountProfile entity.
func (_m *AccountProfile) QueryAccount() *AccountQuery {
	return NewAccountProfileClient(_m.config).QueryAccount(_m)
}

// Update returns a builder for updating this AccountProfile.
// Note that you need to call AccountProfile.Unwrap() before calling this method if this AccountProfile
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *AccountProfile) Update() *AccountProfileUpdateOne {
	return NewAccountProfileClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the AccountProfile entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *AccountProfile) Unwrap() *AccountProfile {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: AccountProfile is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *AccountProfile) String() string {
	var builder strings.Builder
	builder.WriteString("AccountProfile(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("account_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.AccountID))
	builder.WriteString(", ")
	builder.WriteString("normalized_handle=")
	builder.WriteString(_m.NormalizedHandle)
	builder.WriteString(", ")
	builder.WriteString("display_name=")
	builder.WriteString(_m.DisplayName)
	builder.WriteString(", ")
	builder.WriteString("published=")
	builder.WriteString(fmt.Sprintf("%v", _m.Published))
	builder.WriteString(", ")
	builder.WriteString("selected_entries=")
	builder.WriteString(fmt.Sprintf("%v", _m.SelectedEntries))
	builder.WriteByte(')')
	return builder.String()
}

// AccountProfiles is a parsable slice of AccountProfile.
type AccountProfiles []*AccountProfile
