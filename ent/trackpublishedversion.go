// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/dotwaffle/beamers/ent/track"
	"github.com/dotwaffle/beamers/ent/trackpublishedversion"
)

// TrackPublishedVersion is the model entity for the TrackPublishedVersion schema.
type TrackPublishedVersion struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// TrackID holds the value of the "track_id" field.
	TrackID int `json:"track_id,omitempty"`
	// PublishedRevision holds the value of the "published_revision" field.
	PublishedRevision int `json:"published_revision,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Retired holds the value of the "retired" field.
	Retired bool `json:"retired,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TrackPublishedVersionQuery when eager-loading is set.
	Edges        TrackPublishedVersionEdges `json:"edges"`
	selectValues sql.SelectValues
}

// TrackPublishedVersionEdges holds the relations/edges for other nodes in the graph.
type TrackPublishedVersionEdges struct {
	// Track holds the value of the track edge.
	Track *Track `json:"track,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// TrackOrErr returns the Track value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TrackPublishedVersionEdges) TrackOrErr() (*Track, error) {
	if e.Track != nil {
		return e.Track, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: track.Label}
	}
	return nil, &NotLoadedError{edge: "track"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TrackPublishedVersion) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case trackpublishedversion.FieldRetired:
			values[i] = new(sql.NullBool)
		case trackpublishedversion.FieldID, trackpublishedversion.FieldTrackID, trackpublishedversion.FieldPublishedRevision:
			values[i] = new(sql.NullInt64)
		case trackpublishedversion.FieldName:
			values[i] = new(sql.NullString)
		case trackpublishedversion.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TrackPublishedVersion fields.
func (_m *TrackPublishedVersion) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case trackpublishedversion.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case trackpublishedversion.FieldTrackID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field track_id", values[i])
			} else if value.Valid {
				_m.TrackID = int(value.Int64)
			}
		case trackpublishedversion.FieldPublishedRevision:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field published_revision", values[i])
			} else if value.Valid {
				_m.PublishedRevision = int(value.Int64)
			}
		case trackpublishedversion.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case trackpublishedversion.FieldRetired:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field retired", values[i])
			} else if value.Valid {
				_m.Retired = value.Bool
			}
		case trackpublishedversion.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TrackPublishedVersion.
// This includes values selected through modifiers, order, etc.
func (_m *TrackPublishedVersion) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryTrack queries the "track" edge of the TrackPublishedVersion entity.
func (_m *TrackPublishedVersion) QueryTrack() *TrackQuery {
	return NewTrackPublishedVersionClient(_m.config).QueryTrack(_m)
}

// Update returns a builder for updating this TrackPublishedVersion.
// Note that you need to call TrackPublishedVersion.Unwrap() before calling this method if this TrackPublishedVersion
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *TrackPublishedVersion) Update() *TrackPublishedVersionUpdateOne {
	return NewTrackPublishedVersionClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the TrackPublishedVersion entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *TrackPublishedVersion) Unwrap() *TrackPublishedVersion {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: TrackPublishedVersion is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *TrackPublishedVersion) String() string {
	var builder strings.Builder
	builder.WriteString("TrackPublishedVersion(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("track_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.TrackID))
	builder.WriteString(", ")
	builder.WriteString("published_revision=")
	builder.WriteString(fmt.Sprintf("%v", _m.PublishedRevision))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("retired=")
	builder.WriteString(fmt.Sprintf("%v", _m.Retired))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// TrackPublishedVersions is a parsable slice of TrackPublishedVersion.
type TrackPublishedVersions []*TrackPublishedVersion
