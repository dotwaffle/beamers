// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/dotwaffle/beamers/ent/display"
	"github.com/dotwaffle/beamers/ent/displaycredential"
)

// DisplayCredential is the model entity for the DisplayCredential schema.
type DisplayCredential struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// DisplayID holds the value of the "display_id" field.
	DisplayID int `json:"display_id,omitempty"`
	// TokenHash holds the value of the "token_hash" field.
	TokenHash string `json:"-"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// RevokedAt holds the value of the "revoked_at" field.
	RevokedAt *time.Time `json:"revoked_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DisplayCredentialQuery when eager-loading is set.
	Edges        DisplayCredentialEdges `json:"edges"`
	selectValues sql.SelectValues
}

// DisplayCredentialEdges holds the relations/edges for other nodes in the graph.
type DisplayCredentialEdges struct {
	// Display holds the value of the display edge.
	Display *Display `json:"display,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// DisplayOrErr returns the Display value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e DisplayCredentialEdges) DisplayOrErr() (*Display, error) {
	if e.Display != nil {
		return e.Display, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: display.Label}
	}
	return nil, &NotLoadedError{edge: "display"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DisplayCredential) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case displaycredential.FieldID, displaycredential.FieldDisplayID:
			values[i] = new(sql.NullInt64)
		case displaycredential.FieldTokenHash:
			values[i] = new(sql.NullString)
		case displaycredential.FieldCreatedAt, displaycredential.FieldRevokedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DisplayCredential fields.
func (_m *DisplayCredential) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case displaycredential.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case displaycredential.FieldDisplayID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field display_id", values[i])
			} else if value.Valid {
				_m.DisplayID = int(value.Int64)
			}
		case displaycredential.FieldTokenHash:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field token_hash", values[i])
			} else if value.Valid {
				_m.TokenHash = value.String
			}
		case displaycredential.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case displaycredential.FieldRevokedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field revoked_at", values[i])
			} else if value.Valid {
				_m.RevokedAt = new(time.Time)
				*_m.RevokedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DisplayCredential.
// This includes values selected through modifiers, order, etc.
func (_m *DisplayCredential) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryDisplay queries the "display" edge of the DisplayCredential entity.
func (_m *DisplayCredential) QueryDisplay() *DisplayQuery {
	return NewDisplayCredentialClient(_m.config).QueryDisplay(_m)
}

// Update returns a builder for updating this DisplayCredential.
// Note that you need to call DisplayCredential.Unwrap() before calling this method if this DisplayCredential
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *DisplayCredential) Update() *DisplayCredentialUpdateOne {
	return NewDisplayCredentialClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the DisplayCredential entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *DisplayCredential) Unwrap() *DisplayCredential {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: DisplayCredential is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *DisplayCredential) String() string {
	var builder strings.Builder
	builder.WriteString("DisplayCredential(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("display_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.DisplayID))
	builder.WriteString(", ")
	builder.WriteString("token_hash=<sensitive>")
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := _m.RevokedAt; v != nil {
		builder.WriteString("revoked_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteByte(')')
	return builder.String()
}

// DisplayCredentials is a parsable slice of DisplayCredential.
type DisplayCredentials []*DisplayCredential
