// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/dotwaffle/beamers/ent/account"
	"github.com/dotwaffle/beamers/ent/event"
	"github.com/dotwaffle/beamers/ent/votingeligibility"
)

// VotingEligibility is the model entity for the VotingEligibility schema.
type VotingEligibility struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// EventID holds the value of the "event_id" field.
	EventID int `json:"event_id,omitempty"`
	// AccountID holds the value of the "account_id" field.
	AccountID int `json:"account_id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the VotingEligibilityQuery when eager-loading is set.
	Edges        VotingEligibilityEdges `json:"edges"`
	selectValues sql.SelectValues
}

// VotingEligibilityEdges holds the relations/edges for other nodes in the graph.
type VotingEligibilityEdges struct {
	// Event holds the value of the event edge.
	Event *Event `json:"event,omitempty"`
	// Account holds the value of the account edge.
	Account *Account `json:"account,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// EventOrErr returns the Event value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e VotingEligibilityEdges) EventOrErr() (*Event, error) {
	if e.Event != nil {
		return e.Event, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: event.Label}
	}
	return nil, &NotLoadedError{edge: "event"}
}

// AccountOrErr returns the Account value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e VotingEligibilityEdges) AccountOrErr() (*Account, error) {
	if e.Account != nil {
		return e.Account, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: account.Label}
	}
	return nil, &NotLoadedError{edge: "account"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*VotingEligibility) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case votingeligibility.FieldID, votingeligibility.FieldEventID, votingeligibility.FieldAccountID:
			values[i] = new(sql.NullInt64)
		case votingeligibility.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the VotingEligibility fields.
func (_m *VotingEligibility) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case votingeligibility.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case votingeligibility.FieldEventID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field event_id", values[i])
			} else if value.Valid {
				_m.EventID = int(value.Int64)
			}
		case votingeligibility.FieldAccountID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field account_id", values[i])
			} else if value.Valid {
				_m.AccountID = int(value.Int64)
			}
		case votingeligibility.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the VotingEligibility.
// This includes values selected through modifiers, order, etc.
func (_m *VotingEligibility) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryEvent queries the "event" edge of the VotingEligibility entity.
func (_m *VotingEligibility) QueryEvent() *EventQuery {
	return NewVotingEligibilityClient(_m.config).QueryEvent(_m)
}

// QueryAccount queries the "account" edge of the VotingEligibility entity.
func (_m *VotingEligibility) QueryAccount() *AccountQuery {
	return NewVotingEligibilityClient(_m.config).QueryAccount(_m)
}

// Update returns a builder for updating this VotingEligibility.
// Note that you need to call VotingEligibility.Unwrap() before calling this method if this VotingEligibility
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *VotingEligibility) Update() *VotingEligibilityUpdateOne {
	return NewVotingEligibilityClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the VotingEligibility entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *VotingEligibility) Unwrap() *VotingEligibility {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: VotingEligibility is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *VotingEligibility) String() string {
	var builder strings.Builder
	builder.WriteString("VotingEligibility(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("event_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.EventID))
	builder.WriteString(", ")
	builder.WriteString("account_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.AccountID))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// VotingEligibilities is a parsable slice of VotingEligibility.
type VotingEligibilities []*VotingEligibility
