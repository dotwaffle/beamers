// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/dotwaffle/beamers/ent/display"
)

// Display is the model entity for the Display schema.
type Display struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// EnrolledAt holds the value of the "enrolled_at" field.
	EnrolledAt time.Time `json:"enrolled_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DisplayQuery when eager-loading is set.
	Edges        DisplayEdges `json:"edges"`
	selectValues sql.SelectValues
}

// DisplayEdges holds the relations/edges for other nodes in the graph.
type DisplayEdges struct {
	// Credentials holds the value of the credentials edge.
	Credentials []*DisplayCredential `json:"credentials,omitempty"`
	// Assignments holds the value of the assignments edge.
	Assignments []*DisplayAssignment `json:"assignments,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// CredentialsOrErr returns the Credentials value or an error if the edge
// was not loaded in eager-loading.
func (e DisplayEdges) CredentialsOrErr() ([]*DisplayCredential, error) {
	if e.loadedTypes[0] {
		return e.Credentials, nil
	}
	return nil, &NotLoadedError{edge: "credentials"}
}

// AssignmentsOrErr returns the Assignments value or an error if the edge
// was not loaded in eager-loading.
func (e DisplayEdges) AssignmentsOrErr() ([]*DisplayAssignment, error) {
	if e.loadedTypes[1] {
		return e.Assignments, nil
	}
	return nil, &NotLoadedError{edge: "assignments"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Display) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case display.FieldID:
			values[i] = new(sql.NullInt64)
		case display.FieldName:
			values[i] = new(sql.NullString)
		case display.FieldCreatedAt, display.FieldEnrolledAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Display fields.
func (_m *Display) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case display.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case display.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case display.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case display.FieldEnrolledAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field enrolled_at", values[i])
			} else if value.Valid {
				_m.EnrolledAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Display.
// This includes values selected through modifiers, order, etc.
func (_m *Display) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryCredentials queries the "credentials" edge of the Display entity.
func (_m *Display) QueryCredentials() *DisplayCredentialQuery {
	return NewDisplayClient(_m.config).QueryCredentials(_m)
}

// QueryAssignments queries the "assignments" edge of the Display entity.
func (_m *Display) QueryAssignments() *DisplayAssignmentQuery {
	return NewDisplayClient(_m.config).QueryAssignments(_m)
}

// Update returns a builder for updating this Display.
// Note that you need to call Display.Unwrap() before calling this method if this Display
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Display) Update() *DisplayUpdateOne {
	return NewDisplayClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Display entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Display) Unwrap() *Display {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Display is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Display) String() string {
	var builder strings.Builder
	builder.WriteString("Display(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("enrolled_at=")
	builder.WriteString(_m.EnrolledAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Displays is a parsable slice of Display.
type Displays []*Display
