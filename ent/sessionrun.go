// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/dotwaffle/beamers/ent/session"
	"github.com/dotwaffle/beamers/ent/sessionrun"
)

// SessionRun is the model entity for the SessionRun schema.
type SessionRun struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// SessionID holds the value of the "session_id" field.
	SessionID int `json:"session_id,omitempty"`
	// ActualStart holds the value of the "actual_start" field.
	ActualStart time.Time `json:"actual_start,omitempty"`
	// ActualEnd holds the value of the "actual_end" field.
	ActualEnd time.Time `json:"actual_end,omitempty"`
	// Outcome holds the value of the "outcome" field.
	Outcome sessionrun.Outcome `json:"outcome,omitempty"`
	// TargetAdjustmentSeconds holds the value of the "target_adjustment_seconds" field.
	TargetAdjustmentSeconds int `json:"target_adjustment_seconds,omitempty"`
	// TargetAdjustedAt holds the value of the "target_adjusted_at" field.
	TargetAdjustedAt time.Time `json:"target_adjusted_at,omitempty"`
	// SnapshotJSON holds the value of the "snapshot_json" field.
	SnapshotJSON string `json:"snapshot_json,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SessionRunQuery when eager-loading is set.
	Edges        SessionRunEdges `json:"edges"`
	selectValues sql.SelectValues
}

// SessionRunEdges holds the relations/edges for other nodes in the graph.
type SessionRunEdges struct {
	// Session holds the value of the session edge.
	Session *Session `json:"session,omitempty"`
	// Amendments holds the value of the amendments edge.
	Amendments []*SessionRunAmendment `json:"amendments,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// SessionOrErr returns the Session value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SessionRunEdges) SessionOrErr() (*Session, error) {
	if e.Session != nil {
		return e.Session, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: session.Label}
	}
	return nil, &NotLoadedError{edge: "session"}
}

// AmendmentsOrErr returns the Amendments value or an error if the edge
// was not loaded in eager-loading.
func (e SessionRunEdges) AmendmentsOrErr() ([]*SessionRunAmendment, error) {
	if e.loadedTypes[1] {
		return e.Amendments, nil
	}
	return nil, &NotLoadedError{edge: "amendments"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SessionRun) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case sessionrun.FieldID, sessionrun.FieldSessionID, sessionrun.FieldTargetAdjustmentSeconds:
			values[i] = new(sql.NullInt64)
		case sessionrun.FieldOutcome, sessionrun.FieldSnapshotJSON:
			values[i] = new(sql.NullString)
		case sessionrun.FieldActualStart, sessionrun.FieldActualEnd, sessionrun.FieldTargetAdjustedAt, sessionrun.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SessionRun fields.
func (_m *SessionRun) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case sessionrun.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case sessionrun.FieldSessionID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field session_id", values[i])
			} else if value.Valid {
				_m.SessionID = int(value.Int64)
			}
		case sessionrun.FieldActualStart:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field actual_start", values[i])
			} else if value.Valid {
				_m.ActualStart = value.Time
			}
		case sessionrun.FieldActualEnd:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field actual_end", values[i])
			} else if value.Valid {
				_m.ActualEnd = value.Time
			}
		case sessionrun.FieldOutcome:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field outcome", values[i])
			} else if value.Valid {
				_m.Outcome = sessionrun.Outcome(value.String)
			}
		case sessionrun.FieldTargetAdjustmentSeconds:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field target_adjustment_seconds", values[i])
			} else if value.Valid {
				_m.TargetAdjustmentSeconds = int(value.Int64)
			}
		case sessionrun.FieldTargetAdjustedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field target_adjusted_at", values[i])
			} else if value.Valid {
				_m.TargetAdjustedAt = value.Time
			}
		case sessionrun.FieldSnapshotJSON:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field snapshot_json", values[i])
			} else if value.Valid {
				_m.SnapshotJSON = value.String
			}
		case sessionrun.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SessionRun.
// This includes values selected through modifiers, order, etc.
func (_m *SessionRun) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QuerySession queries the "session" edge of the SessionRun entity.
func (_m *SessionRun) QuerySession() *SessionQuery {
	return NewSessionRunClient(_m.config).QuerySession(_m)
}

// QueryAmendments queries the "amendments" edge of the SessionRun entity.
func (_m *SessionRun) QueryAmendments() *SessionRunAmendmentQuery {
	return NewSessionRunClient(_m.config).QueryAmendments(_m)
}

// Update returns a builder for updating this SessionRun.
// Note that you need to call SessionRun.Unwrap() before calling this method if this SessionRun
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *SessionRun) Update() *SessionRunUpdateOne {
	return NewSessionRunClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the SessionRun entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *SessionRun) Unwrap() *SessionRun {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: SessionRun is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *SessionRun) String() string {
	var builder strings.Builder
	builder.WriteString("SessionRun(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("session_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.SessionID))
	builder.WriteString(", ")
	builder.WriteString("actual_start=")
	builder.WriteString(_m.ActualStart.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("actual_end=")
	builder.WriteString(_m.ActualEnd.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("outcome=")
	builder.WriteString(fmt.Sprintf("%v", _m.Outcome))
	builder.WriteString(", ")
	builder.WriteString("target_adjustment_seconds=")
	builder.WriteString(fmt.Sprintf("%v", _m.TargetAdjustmentSeconds))
	builder.WriteString(", ")
	builder.WriteString("target_adjusted_at=")
	builder.WriteString(_m.TargetAdjustedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("snapshot_json=")
	builder.WriteString(_m.SnapshotJSON)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SessionRuns is a parsable slice of SessionRun.
type SessionRuns []*SessionRun
