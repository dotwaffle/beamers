// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/dotwaffle/beamers/ent/event"
	"github.com/dotwaffle/beamers/ent/lane"
	"github.com/dotwaffle/beamers/ent/lanedraft"
)

// Lane is the model entity for the Lane schema.
type Lane struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// EventID holds the value of the "event_id" field.
	EventID int `json:"event_id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the LaneQuery when eager-loading is set.
	Edges        LaneEdges `json:"edges"`
	selectValues sql.SelectValues
}

// LaneEdges holds the relations/edges for other nodes in the graph.
type LaneEdges struct {
	// Event holds the value of the event edge.
	Event *Event `json:"event,omitempty"`
	// Draft holds the value of the draft edge.
	Draft *LaneDraft `json:"draft,omitempty"`
	// PublishedVersions holds the value of the published_versions edge.
	PublishedVersions []*LanePublishedVersion `json:"published_versions,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// EventOrErr returns the Event value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e LaneEdges) EventOrErr() (*Event, error) {
	if e.Event != nil {
		return e.Event, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: event.Label}
	}
	return nil, &NotLoadedError{edge: "event"}
}

// DraftOrErr returns the Draft value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e LaneEdges) DraftOrErr() (*LaneDraft, error) {
	if e.Draft != nil {
		return e.Draft, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: lanedraft.Label}
	}
	return nil, &NotLoadedError{edge: "draft"}
}

// PublishedVersionsOrErr returns the PublishedVersions value or an error if the edge
// was not loaded in eager-loading.
func (e LaneEdges) PublishedVersionsOrErr() ([]*LanePublishedVersion, error) {
	if e.loadedTypes[2] {
		return e.PublishedVersions, nil
	}
	return nil, &NotLoadedError{edge: "published_versions"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Lane) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case lane.FieldID, lane.FieldEventID:
			values[i] = new(sql.NullInt64)
		case lane.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Lane fields.
func (_m *Lane) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case lane.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case lane.FieldEventID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field event_id", values[i])
			} else if value.Valid {
				_m.EventID = int(value.Int64)
			}
		case lane.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Lane.
// This includes values selected through modifiers, order, etc.
func (_m *Lane) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryEvent queries the "event" edge of the Lane entity.
func (_m *Lane) QueryEvent() *EventQuery {
	return NewLaneClient(_m.config).QueryEvent(_m)
}

// QueryDraft queries the "draft" edge of the Lane entity.
func (_m *Lane) QueryDraft() *LaneDraftQuery {
	return NewLaneClient(_m.config).QueryDraft(_m)
}

// QueryPublishedVersions queries the "published_versions" edge of the Lane entity.
func (_m *Lane) QueryPublishedVersions() *LanePublishedVersionQuery {
	return NewLaneClient(_m.config).QueryPublishedVersions(_m)
}

// Update returns a builder for updating this Lane.
// Note that you need to call Lane.Unwrap() before calling this method if this Lane
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Lane) Update() *LaneUpdateOne {
	return NewLaneClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Lane entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Lane) Unwrap() *Lane {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Lane is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Lane) String() string {
	var builder strings.Builder
	builder.WriteString("Lane(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("event_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.EventID))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Lanes is a parsable slice of Lane.
type Lanes []*Lane
