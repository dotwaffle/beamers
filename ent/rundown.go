// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/dotwaffle/beamers/ent/event"
	"github.com/dotwaffle/beamers/ent/rundown"
)

// Rundown is the model entity for the Rundown schema.
type Rundown struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// EventID holds the value of the "event_id" field.
	EventID int `json:"event_id,omitempty"`
	// DraftRevision holds the value of the "draft_revision" field.
	DraftRevision int `json:"draft_revision,omitempty"`
	// PublishedRevision holds the value of the "published_revision" field.
	PublishedRevision int `json:"published_revision,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the RundownQuery when eager-loading is set.
	Edges        RundownEdges `json:"edges"`
	selectValues sql.SelectValues
}

// RundownEdges holds the relations/edges for other nodes in the graph.
type RundownEdges struct {
	// Event holds the value of the event edge.
	Event *Event `json:"event,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// EventOrErr returns the Event value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e RundownEdges) EventOrErr() (*Event, error) {
	if e.Event != nil {
		return e.Event, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: event.Label}
	}
	return nil, &NotLoadedError{edge: "event"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Rundown) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case rundown.FieldID, rundown.FieldEventID, rundown.FieldDraftRevision, rundown.FieldPublishedRevision:
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Rundown fields.
func (_m *Rundown) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case rundown.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case rundown.FieldEventID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field event_id", values[i])
			} else if value.Valid {
				_m.EventID = int(value.Int64)
			}
		case rundown.FieldDraftRevision:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field draft_revision", values[i])
			} else if value.Valid {
				_m.DraftRevision = int(value.Int64)
			}
		case rundown.FieldPublishedRevision:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field published_revision", values[i])
			} else if value.Valid {
				_m.PublishedRevision = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Rundown.
// This includes values selected through modifiers, order, etc.
func (_m *Rundown) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryEvent queries the "event" edge of the Rundown entity.
func (_m *Rundown) QueryEvent() *EventQuery {
	return NewRundownClient(_m.config).QueryEvent(_m)
}

// Update returns a builder for updating this Rundown.
// Note that you need to call Rundown.Unwrap() before calling this method if this Rundown
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Rundown) Update() *RundownUpdateOne {
	return NewRundownClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Rundown entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Rundown) Unwrap() *Rundown {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Rundown is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Rundown) String() string {
	var builder strings.Builder
	builder.WriteString("Rundown(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("event_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.EventID))
	builder.WriteString(", ")
	builder.WriteString("draft_revision=")
	builder.WriteString(fmt.Sprintf("%v", _m.DraftRevision))
	builder.WriteString(", ")
	builder.WriteString("published_revision=")
	builder.WriteString(fmt.Sprintf("%v", _m.PublishedRevision))
	builder.WriteByte(')')
	return builder.String()
}

// Rundowns is a parsable slice of Rundown.
type Rundowns []*Rundown
