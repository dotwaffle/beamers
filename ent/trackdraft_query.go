// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/dotwaffle/beamers/ent/predicate"
	"github.com/dotwaffle/beamers/ent/track"
	"github.com/dotwaffle/beamers/ent/trackdraft"
)

// TrackDraftQuery is the builder for querying TrackDraft entities.
type TrackDraftQuery struct {
	config
	ctx        *QueryContext
	order      []trackdraft.OrderOption
	inters     []Interceptor
	predicates []predicate.TrackDraft
	withTrack  *TrackQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TrackDraftQuery builder.
func (_q *TrackDraftQuery) Where(ps ...predicate.TrackDraft) *TrackDraftQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *TrackDraftQuery) Limit(limit int) *TrackDraftQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *TrackDraftQuery) Offset(offset int) *TrackDraftQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *TrackDraftQuery) Unique(unique bool) *TrackDraftQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *TrackDraftQuery) Order(o ...trackdraft.OrderOption) *TrackDraftQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryTrack chains the current query on the "track" edge.
func (_q *TrackDraftQuery) QueryTrack() *TrackQuery {
	query := (&TrackClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(trackdraft.Table, trackdraft.FieldID, selector),
			sqlgraph.To(track.Table, track.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, trackdraft.TrackTable, trackdraft.TrackColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first TrackDraft entity from the query.
// Returns a *NotFoundError when no TrackDraft was found.
func (_q *TrackDraftQuery) First(ctx context.Context) (*TrackDraft, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{trackdraft.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *TrackDraftQuery) FirstX(ctx context.Context) *TrackDraft {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TrackDraft ID from the query.
// Returns a *NotFoundError when no TrackDraft ID was found.
func (_q *TrackDraftQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{trackdraft.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *TrackDraftQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TrackDraft entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TrackDraft entity is found.
// Returns a *NotFoundError when no TrackDraft entities are found.
func (_q *TrackDraftQuery) Only(ctx context.Context) (*TrackDraft, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{trackdraft.Label}
	default:
		return nil, &NotSingularError{trackdraft.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *TrackDraftQuery) OnlyX(ctx context.Context) *TrackDraft {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TrackDraft ID in the query.
// Returns a *NotSingularError when more than one TrackDraft ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *TrackDraftQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{trackdraft.Label}
	default:
		err = &NotSingularError{trackdraft.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *TrackDraftQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TrackDrafts.
func (_q *TrackDraftQuery) All(ctx context.Context) ([]*TrackDraft, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*TrackDraft, *TrackDraftQuery]()
	return withInterceptors[[]*TrackDraft](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *TrackDraftQuery) AllX(ctx context.Context) []*TrackDraft {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TrackDraft IDs.
func (_q *TrackDraftQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(trackdraft.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *TrackDraftQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *TrackDraftQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*TrackDraftQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *TrackDraftQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *TrackDraftQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *TrackDraftQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TrackDraftQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *TrackDraftQuery) Clone() *TrackDraftQuery {
	if _q == nil {
		return nil
	}
	return &TrackDraftQuery{
		config:     _q.config,
		ctx:        _q.ctx.Clone(),
		order:      append([]trackdraft.OrderOption{}, _q.order...),
		inters:     append([]Interceptor{}, _q.inters...),
		predicates: append([]predicate.TrackDraft{}, _q.predicates...),
		withTrack:  _q.withTrack.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithTrack tells the query-builder to eager-load the nodes that are connected to
// the "track" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *TrackDraftQuery) WithTrack(opts ...func(*TrackQuery)) *TrackDraftQuery {
	query := (&TrackClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTrack = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TrackID int `json:"track_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TrackDraft.Query().
//		GroupBy(trackdraft.FieldTrackID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *TrackDraftQuery) GroupBy(field string, fields ...string) *TrackDraftGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &TrackDraftGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = trackdraft.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TrackID int `json:"track_id,omitempty"`
//	}
//
//	client.TrackDraft.Query().
//		Select(trackdraft.FieldTrackID).
//		Scan(ctx, &v)
func (_q *TrackDraftQuery) Select(fields ...string) *TrackDraftSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &TrackDraftSelect{TrackDraftQuery: _q}
	sbuild.label = trackdraft.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TrackDraftSelect configured with the given aggregations.
func (_q *TrackDraftQuery) Aggregate(fns ...AggregateFunc) *TrackDraftSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *TrackDraftQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !trackdraft.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	if trackdraft.Policy == nil {
		return errors.New("ent: uninitialized trackdraft.Policy (forgotten import ent/runtime?)")
	}
	if err := trackdraft.Policy.EvalQuery(ctx, _q); err != nil {
		return err
	}
	return nil
}

func (_q *TrackDraftQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TrackDraft, error) {
	var (
		nodes       = []*TrackDraft{}
		_spec       = _q.querySpec()
		loadedTypes = [1]bool{
			_q.withTrack != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TrackDraft).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TrackDraft{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withTrack; query != nil {
		if err := _q.loadTrack(ctx, query, nodes, nil,
			func(n *TrackDraft, e *Track) { n.Edges.Track = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *TrackDraftQuery) loadTrack(ctx context.Context, query *TrackQuery, nodes []*TrackDraft, init func(*TrackDraft), assign func(*TrackDraft, *Track)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*TrackDraft)
	for i := range nodes {
		fk := nodes[i].TrackID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(track.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "track_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *TrackDraftQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *TrackDraftQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(trackdraft.Table, trackdraft.Columns, sqlgraph.NewFieldSpec(trackdraft.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, trackdraft.FieldID)
		for i := range fields {
			if fields[i] != trackdraft.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withTrack != nil {
			_spec.Node.AddColumnOnce(trackdraft.FieldTrackID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *TrackDraftQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(trackdraft.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = trackdraft.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// TrackDraftGroupBy is the group-by builder for TrackDraft entities.
type TrackDraftGroupBy struct {
	selector
	build *TrackDraftQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *TrackDraftGroupBy) Aggregate(fns ...AggregateFunc) *TrackDraftGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *TrackDraftGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TrackDraftQuery, *TrackDraftGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *TrackDraftGroupBy) sqlScan(ctx context.Context, root *TrackDraftQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TrackDraftSelect is the builder for selecting fields of TrackDraft entities.
type TrackDraftSelect struct {
	*TrackDraftQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *TrackDraftSelect) Aggregate(fns ...AggregateFunc) *TrackDraftSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *TrackDraftSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TrackDraftQuery, *TrackDraftSelect](ctx, _s.TrackDraftQuery, _s, _s.inters, v)
}

func (_s *TrackDraftSelect) sqlScan(ctx context.Context, root *TrackDraftQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
