# Enroll Displays without device input

An unpaired Display may show a short-lived, single-use QR code and human-readable code.
An authorized Crew Member claims that code from another device, then names the Display and assigns its Location and normal View.
The code identifies the Display but grants no crew authority; successful Enrollment issues a persistent Display credential.
Manual code entry and pre-provisioned configuration remain supported alternatives.
