# Run live authority at the Venue

The initial system runs one authoritative Go service on the Event's local network and serves both crew interfaces and browser Displays there.
Live operation does not depend on internet access, avoiding venue connectivity as a single point of failure and keeping Display updates low latency.
Optional cloud backup, remote access, or hosted operation may be added later without becoming the default live authority.
