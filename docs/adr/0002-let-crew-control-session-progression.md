# Let crew control Session progression

An authorized Crew Member explicitly starts and ends each Session, so Displays reflect what is actually happening rather than assuming the Rundown is on time.
Per-Session automation may later provide optional scheduled start and end transitions without changing crew control as the default.
